Metadata-Version: 2.4
Name: professional-coding-env
Version: 3.0.0
Summary: Professional software engineering task simulation with isolated WebSocket sessions
Author: OpenEnv Submitter
License: MIT
Classifier: Programming Language :: Python :: 3
Classifier: Operating System :: OS Independent
Classifier: Framework :: FastAPI
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: pydantic[email]==2.5.0
Requires-Dist: pydantic-settings>=2.0.0
Requires-Dist: fastapi>=0.104.1
Requires-Dist: uvicorn>=0.24.0
Requires-Dist: websockets
Requires-Dist: openai>=1.2.3
Requires-Dist: gradio
Requires-Dist: openenv>=0.1.0
Requires-Dist: sqlalchemy>=2.0.0
Requires-Dist: passlib[bcrypt]>=1.7.4
Requires-Dist: python-jose[cryptography]>=3.3.0
Requires-Dist: prometheus-client>=0.17.1
Requires-Dist: structlog>=23.2.0
Requires-Dist: python-dotenv>=1.0.0
Requires-Dist: astunparse>=1.6.3
Requires-Dist: python-multipart

---
title: professional-coding-env
emoji: 🚀
colorFrom: blue
colorTo: green
sdk: docker
app_file: server/app.py
pinned: false
app_port: 7860
---


# OpenEnv: Professional Coding Task Simulation

OpenEnv is a production-grade playground for evaluating AI software engineering agents on real-world coding tasks. It provides a session-isolated, high-performance environment with integrated graders and structured logging.

## 🚀 Quick Start

### Prerequisites
- Python 3.11+
- [uv](https://github.com/astral-sh/uv) (recommended) or `pip`

### Setup
```bash
# Using uv (recommended)
uv sync

# Using pip
pip install -r requirements.txt
```

### Run Server Locally
```bash
uv run python3 server/app.py
```

## 🛠 API Documentation

The OpenEnv server exposes both REST and WebSocket interfaces for flexible integration.

### REST Endpoints
- `GET /health`: Health check and system metrics.
- `POST /reset`: Initialize a new session. Returns `session_id` and the initial `observation`.
- `POST /step`: Execute an action in an active session. Requires `session_id` and `action_data`.
- `GET /state?session_id=<id>`: Retrieve full internal state for auditing/debugging.

### WebSocket Interface
Connect to `ws://<host>:<port>/ws` for persistent, low-latency session management.
- Protocol: JSON-based message exchange.
- Message Types: `reset`, `step`, `state`.

## 🧠 Core Tasks
OpenEnv includes 3 distinct real-world tasks with automated graders:
1. **Email Triage (Easy)**: Filtering urgent tickets from a support stream.
2. **Data Pipeline (Medium)**: Cleaning and imputing customer datasets using pandas.
3. **Security Patch (Hard)**: Remediating SQL injection vulnerabilities via parameterized queries.

## 📊 Baseline Reproduction
To run the reference baseline and verify structured logging compliance:
```bash
export API_BASE_URL="https://api.openai.com/v1"
export MODEL_NAME="gpt-3.5-turbo"
export HF_TOKEN="your-api-key"
uv run python3 inference.py
```

## 🔍 Troubleshooting
- **Connection Refused**: Ensure the server is running and the port (default 7860) is exposed.
- **ModuleNotFoundError**: Ensure you are using `uv run` or have activated your virtual environment.
- **Score is 0.0**: Check the grader logic in `tasks.py` and ensure the agent response contains the expected patterns.

## 🛡 Security & Compliance
- **Zero-knowledge Reset**: State is wiped using cryptographically secure nonces to prevent leakage between episodes.
- **Thread-safe**: Session management utilizes asynchronous locks to ensure integrity under concurrent load.
- **Non-root Docker**: Containers run as a limited user to minimize attack surfaces.
